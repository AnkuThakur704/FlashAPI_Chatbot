{
  "name": "backend",
  "version": "1.0.0",
  "description": "this is the backend for ai chatbot made using gemini-flash-2.5",
  "main": "index.js",
  "type": "module",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node run script",
    "dev": "node --watch --env-file=.env.local script"
  },
  "author": "Ankush",
  "license": "MIT",
  "dependencies": {
    "@google/genai": "^1.34.0",
    "cors": "^2.8.5",
    "express": "^5.2.1"
  }
}
